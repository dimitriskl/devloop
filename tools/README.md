# Tools

This standalone bundle includes only tools required by the development loop.

The SQL diagnostics MCP source is placed under:

```text
mcp/sql_diagnostics/
```

Other target repository tools are not copied because they either depend on
the full target repository test projects or are feature-specific utilities
that are not needed for the portable loop.


